<div class = "container">
<%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill %>
<h1 class = "text-white" > 🍹 <%= @cocktail.name %></h1>
<h3 class = "m-3 text-primary">Ingredients:</h3>
<ul>
<h2> <% @cocktail.doses.each do |dose| %> </h2>
  <li class = "text-white"> <%= dose.ingredient.name.capitalize %> </li>
  <p class = "text-white"> <%=   dose.description %> </p>
  <% end %>
</ul>
<%= link_to "Add a dose", new_cocktail_dose_path(@cocktail), class: "btn btn-info m-3"%>

<%= link_to "All cocktails", cocktails_path, class: "btn btn-success m-1"%>
</div>
